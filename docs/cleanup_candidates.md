# Cleanup Candidates

This file records files or folders that may be obsolete, duplicated, or better moved to a legacy area. Nothing listed here has been deleted automatically.

| path | current function | why it may be obsolete | suggested action | deletion risk | user confirmation needed |
| --- | --- | --- | --- | --- | --- |
| `scripts/run_baselines_corrected_demo.py` | older traditional-baseline demo runner | overlaps with newer improved baseline and report scripts | `move_to_legacy` | low | no |
| `scripts/plot_baselines_corrected_demo.py` | plotting for corrected baseline demo | likely superseded by newer baseline comparison reports | `move_to_legacy` | low | no |
| `scripts/run_baselines_ensemble.py` | older ensemble baseline runner | the project now uses neural + analytic inverse estimators rather than classical ensemble baselines | `move_to_legacy` | low | no |
| `scripts/plot_baselines_ensemble.py` | plotting for ensemble baselines | likely redundant with existing summary reports | `move_to_legacy` | low | no |
| `scripts/run_baselines_improved.py` | improved classical baselines | still scientifically useful as traditional-method reference | `keep` | medium | no |
| `scripts/plot_improved_baselines.py` | plots improved classical baselines | still useful for historical comparison | `keep` | medium | no |
| `scripts/train_mlp.py` | old starter training entry point | superseded by `train_spectrum_baseline.py` | `move_to_legacy` | low | no |
| `scripts/plot_baselines.py` | early baseline plotting | functionality appears partly superseded by richer report scripts | `move_to_legacy` | low | no |
| `outputs/dl_spectrum_lmmi_curvature*` | failed curvature-branch experiments | retained as negative results, but not part of final recommended path | `keep` | medium | no |
| `outputs/dl_spectrum_lmmi_ident_h_control` | control experiment checkpoint | useful as identifiability control evidence, but not deployable | `keep` | medium | no |
| `outputs/reports/market_price_preprocessing_summary.*` | market CSV scan report with no market files found | can be regenerated when price CSVs arrive | `keep` for now | low | no |
| `experiments/evaluatE_cmin_robust_multilength.py` | mistaken mixed-case duplicate entry file | replaced by correctly named `evaluate_cmin_robust_multilength.py` | `delete_after_confirmation` | very low | yes |
| `__pycache__/` under `src/` and `scripts/` | Python bytecode cache | generated artifacts, not source | `delete_after_confirmation` | very low | yes |
| `新建 文本文档.txt` | empty stray text file in project root | does not appear to serve any project function | `delete_after_confirmation` | very low | yes |

## Notes

- Checkpoints, result tables, and report files are being kept unless there is a strong reason to remove them.
- Some older scripts may still be referenced informally by notebooks or ad hoc user workflows, so moving them to `legacy/` is safer than deleting them.

## Additional Submission-Bundle Cleanup Notes

These are not deletion requests. They are packaging recommendations after the
supplemental TODO evidence pass.

| path | current function | why it may be excluded or archived | suggested action | deletion risk | user confirmation needed |
| --- | --- | --- | --- | --- | --- |
| `outputs/dl_spectrum_pc_smin_v2_no_gate*` | historical ablation outputs | useful only for legacy ablation tracking, not final main paper path | `archive_to_legacy_after_submission` | medium | yes |
| `paper_writing_workspace/.history` | editor history | not needed for paper submission bundle | `exclude_from_submission_bundle` | low | no |
| `paper_writing_workspace/main.aux`, `main.bbl`, `main.blg`, `main.log`, `main.out` | LaTeX build artifacts | regenerated by local build and not part of manuscript source | `exclude_from_submission_bundle` | low | no |

## Items Requiring Explicit User Confirmation Before Deletion

- All `__pycache__/` folders
- `新建 文本文档.txt`
